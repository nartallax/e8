// that's for Parcel. apparently he's not smart enough to detect them automatically. (why?)
// TODO: remove after esbuild
/// <reference types="./common/modules" />

export type XY = {
	readonly x: number
	readonly y: number
}